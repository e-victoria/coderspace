import { TransformBurgerMenuDirective } from './transform-burger-menu.directive';

describe('TransformBurgerMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new TransformBurgerMenuDirective();
    expect(directive).toBeTruthy();
  });
});
